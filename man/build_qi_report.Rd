% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qi_timeliness.R
\name{build_qi_report}
\alias{build_qi_report}
\title{Build a Timeliness Summary Report}
\usage{
build_qi_report(specimen_df, output_path = NULL)
}
\arguments{
\item{specimen_df}{A data frame with one row per specimen (see
\code{\link[=calculate_qi_intervals]{calculate_qi_intervals()}} for required columns).}

\item{output_path}{Optional. If provided, an .xlsx file is written
to this path. If NULL (the default), no file is written - the
summary tables are just returned as a list.}
}
\value{
A named list of two summary tibbles: \code{birth_to_collection}
and \code{collection_to_report}.
}
\description{
Runs \code{\link[=calculate_qi_intervals]{calculate_qi_intervals()}} on specimen-level data, then
summarizes counts in each timeliness category and (optionally)
writes the result to an Excel workbook - one sheet per interval,
matching the shape of a real NewSTEPs/Title V QI submission.
}
\examples{
data(specimen_data)
summary_tables <- build_qi_report(specimen_data)
summary_tables$birth_to_collection

}
